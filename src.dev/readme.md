### the container
- it has to be support a schema generated or based on the internal standard, the describes said events ... and the the multitude of these..
- the data is in binary with a lookup IL to generalise it.. or its entirely free/open format.
- it might contain if primary format protected data hashed in some way... 
