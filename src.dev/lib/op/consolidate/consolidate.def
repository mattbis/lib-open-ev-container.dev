#If SRC.TIER
#If SRC.TIER IsValid
#If SRC.TIER CanSetRemainder
#If USER AckOverride
 # If SRC.MANIFEST CanConsolidateUnprotected
#For E<T> where:
# common(Infinity) 
do 
#  FastMerge
 # do remove globals to local tier path
 # do remove unused chunks 
 # do remove unused refs
